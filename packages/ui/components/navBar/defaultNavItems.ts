import type { NavItem } from "./nav";

export const APP_BRAND_NAME = "Neural";

/** Free ThemeWagon build — no post-login navigation. */
export function getVaultDockItems(): NavItem[] {
  return [];
}

export function getDefaultNavItems(): NavItem[] {
  return [];
}
