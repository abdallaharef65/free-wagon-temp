# api

Shared API utilities using TanStack Query. Exports `ApiProvider` and `apiFetch`.

Usage:

- Wrap your app root with `ApiProvider` (already wired in mobile and web layouts).
- Use `@tanstack/react-query` hooks in app code.
